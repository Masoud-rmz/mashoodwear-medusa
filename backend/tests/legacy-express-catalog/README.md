/**
 * Legacy Express catalog test folder.
 * Default `npm test` / `npm run test:unit` do **not** run these.
 * Catalog + commerce admin live on Medusa; these only cover residual Express routes.
 *
 *   npm run test:legacy-express-catalog
 */
